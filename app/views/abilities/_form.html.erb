<%= form_for @ability, url: @path_to_save, method: 'post' do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit 'Salvar' %>
<% end %>