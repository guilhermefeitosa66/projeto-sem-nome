<h1>Login</h1>
<hr>
<%= form_for @user, url: start_session_path, method: 'post' do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email, autofocus: 'true' %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit 'Login' %>
<% end %>